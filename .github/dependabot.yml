version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
      day: monday
    # No grouping — individual PRs per action allow the auto-merge workflow
    # to safely merge each SHA update independently after CI passes.
