apiVersion: v2
name: k8s-health-agent
description: A Helm Chart for deploying the K8s Health Agent
type: application
version: 0.19.0 # the helm chart version
appVersion: "v0.5.4" # the ske health agent version
