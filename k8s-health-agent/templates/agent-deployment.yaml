{{- define "patch" }}
spec:
  template:
    spec:
      containers:
        {{- .container | list | toYaml | nindent 8 }}
      {{- with .Values.healthAgent.nodeSelector }}
      nodeSelector:
        {{- . | toYaml | nindent 8 }}
      {{- end }}
      {{- with .Values.healthAgent.tolerations }}
      tolerations:
        {{- . | toYaml | nindent 8 }}
      {{- end }}
      {{- with .Values.healthAgent.affinity }}
      affinity:
        {{- . | toYaml | nindent 8 }}
      {{- end }}
      imagePullSecrets:
        - name: '{{ .Values.imageRegistry.imagePullSecret | default "syntasso-registry" }}'
{{- end }}

{{- define "containerPatch" }}
{{- with .Values.healthAgent.resources }}
resources:
  {{- . | toYaml | nindent 2 }}
{{- end }}
{{- end }}

{{- $manifest := .Files.Get "files/k8s-health-agent-deployment.yaml" | fromYaml }}
{{- $container := index $manifest.spec.template.spec.containers 0
                    | merge (include "containerPatch" . | fromYaml) }}

{{/* Apply image configuration from kustomization.yaml */}}
{{- $originalImage := "registry.syntasso.io/syntasso/k8s-health-agent" }}
{{- $newName := printf "%s/%s" .Values.imageRegistry.host (coalesce .Values.imageRegistry.healthAgentImage.name .Values.imageRegistry.k8sHealthAgentImage.name) }}
{{- $newTag := .Chart.AppVersion }}
{{- $_ := set $container "image" (printf "%s:%s" $newName $newTag) }}
{{- $_ := set $manifest.spec.template "spec"
              (omit $manifest.spec.template.spec "containers" "imagePullSecrets") }}

{{- $manifest
      | merge (include "patch" (merge . (dict "container" $container)) | fromYaml)
      | toYaml }}